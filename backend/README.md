# AskOnce Backend

AI-powered smart reply system with living knowledge base.

## Quick Start

```bash
# Install dependencies
pip install -r requirements.txt

# Copy env file
cp .env.example .env

# Run dev server
uvicorn app.main:app --reload --port 8000
```

## API Docs

Once running, visit: http://localhost:8000/docs
